include "common.fbs";

table ChunkMetadata {
    dtype: DType;
    number_of_elements: ulong;
    number_of_bytes: ulong;
}

table ChunkData {
    data: [ubyte];
}

table Chunk {
    metadata: ChunkMetadata;
    data: ChunkData;
}

root_type Chunk;