include "snapshot.fbs";

table CommitMetadata {
    timestamp: ulong;
    author: string;
    message: string;
}

table Commit {
    metadata: CommitMetadata;
    snapshot: Snapshot;
    parent_commit_refs: [string];
}

root_type Commit;